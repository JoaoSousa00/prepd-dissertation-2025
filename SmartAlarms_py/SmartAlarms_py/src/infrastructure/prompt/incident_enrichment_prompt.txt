You are analyzing one active incident and historical context from the same platform.

Main incident:
- Incident ID: {incident_id}
- Short Description: {short_description}
- Description: {description}
- Full sanitized incident context:
{main_incident_context}

Explicitly referenced related incidents from the main incident:
{related_incident_context}

Same-title historical incidents (same short_description as the main incident):
{same_title_incident_context}

Relevant Confluence documentation used for this analysis. Each entry identifies its source page and summarized content:
{confluence_context}

Instructions:
1. Produce a concise, formal, human-readable summary in 1-2 sentences.
2. Focus on the operational issue, expected impact, and what is useful for the support or development team to understand quickly.
3. When available in the incident context, include the affected Hub and Environment in the summary (for example, EMEA PROD).
4. Do not include low-value administrative metadata such as state, priority, impact, urgency, or assignment team in the summary.
5. Ground any mitigation or investigation suggestion in the historical evidence and Confluence context provided above.
6. Before generating generic mitigation suggestions, assess whether the historical incidents show that this issue should be redirected or reassigned. A redirect is supported when a relevant historical incident has a different assignment_group.name from the current incident and/or its work_notes or comments state that it was sent to another team. When clearly supported, make redirecting to the identified team the first suggestion and cite the supporting incident number(s).
7. Treat the Confluence documentation as a helpful indicator, not a guaranteed source of truth. It may be outdated, incomplete, or only partially relevant, so validate it against the incident facts before using it as evidence.
8. Every suggestion must contain exactly these three parts in order:
   a) Investigation suggestion
   b) Mitigation suggestion
   c) Suggested resolution note for the developer to adapt when closing the incident
9. Return at least 3 mitigation suggestions whenever possible.
10. If historical related incidents are not sufficient to produce 3 fully evidence-backed suggestions, complete the remaining suggestions with meaningful best-practice reasoning based on the current incident details.
11. For each suggestion, explicitly label confidence as either "evidence-based" (supported by historical incidents) or "reasoned fallback" (derived from current-incident analysis due to insufficient history).
12. Every suggestion must explicitly state the source incident number(s) that support it. For reasoned fallback suggestions with no supporting historical incident, use "Sources: current_incident_analysis".
13. Prioritize the most recent relevant closed incidents with meaningful resolution notes when deriving mitigation and investigation guidance.
14. Never invent incident references or include the original incident ID ({incident_id}) in any related_incidents list.
15. For each mitigation suggestion, add a related_pages entry only for Confluence pages whose summarized content was actually used as evidence for that suggestion. Copy the page title and URL exactly from the Confluence documentation context. Do not invent, modify, or include unused pages. Use an empty array when no Confluence page was used.
16. Return valid JSON with the following shape:
   {{
     "summary": "...",
     "related_incidents": ["INC000123", "INC000456"],
     "mitigation_suggestions": [
       {{
         "Confidence": "evidence-based|reasoned fallback",
         "Investigation": "...",
         "Mitigation": "...",
         "Resolution_note": "...",
         "related_incidents": ["INC000123"],
         "related_pages": [
           {{"title": "Page title", "url": "https://..."}}
         ],
         "related_log_ids": []
       }}
     ]
   }}

Important: "related_incidents" should only contain external incident IDs that are relevant historical evidence, not the current incident itself. Confluence pages are advisory only and should not be treated as absolute truth.
